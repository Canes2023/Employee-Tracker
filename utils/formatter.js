// utils/formatter.js

const formatTable = (data) => {
    // Implement your own logic to format data into a table
    // Example: You can use a library like cli-table or tabular to format data into a table.
  };
  
  module.exports = formatTable;